services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: chronotranslate
      POSTGRES_PASSWORD: chronotranslate
      POSTGRES_DB: chronotranslate
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
